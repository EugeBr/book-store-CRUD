<div class="container">
    <h1>Books List</h1>
    {{#each books}}
        <div class="book-row">
            <a href="/books/{{this._id}}">{{this.title}}</a>
            <a href="/books/{{this._id}}/edit" class="btn btn-dark">Edit</a>
            <form action="/books/{{this._id}}/delete"></form>
                <button type="submit" class="btn btn-warning">Delete</button>
        </div>
    {{/each}}
</div>